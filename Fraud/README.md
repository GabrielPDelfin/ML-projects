This is a machine learning model (XGBoost) for fraud detection purposes that was trained with sample data from an .arff file.

Data cleaning procedures were performed before training the ML model.

The importance of each ML feature when making predictions was quantified using SHAP values. They can be visualized in a bar plot.
